Fix applied and validated:
- **Added**: `Set<string>` tracking real paths via `fs.realpath()` before each directory read
- Type-checked: zero `tsc` errors in `file.ts`
- Signature preserved, error handling unchanged